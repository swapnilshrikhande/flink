//
// accept input string
// split based on | (pipes)
// if list is abort
// create deferred object
// loop over each command
// find if command starts with : -> parse and run the command
// else if not :, find function for command and execute it
// chain to earlier command using then


//projects
Lightweight

0. platform: error handling, session, logging/debugging.

1. user ui, keyboard shortcut for daily tasks, plain type and drop down, upload saved workflow.
   chrome plugin

2. admin editor flows ; to design nested complicated flows, allow saving flows to json.

3. salesforce specific plugins (which will work with ui and flows)

5. firefox plugin

6. forum to upload your plugins, flows.

7. How can we best support mobiles ?
